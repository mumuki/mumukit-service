require 'spec_helper'

describe Mumukit::Service do
  it 'has a version number' do
    expect(Mumukit::Service::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
